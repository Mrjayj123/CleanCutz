import React from 'react'

// Placeholder component.
// Your current Studio layout doesn’t use a VideoHistory panel in the existing editor UI.
// This file exists to satisfy imports and prevent build/runtime failures.
export default function VideoHistory() {
  return null
}

